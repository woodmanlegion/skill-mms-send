#!/data/data/com.termux/files/usr/bin/bash
# mms-send — dispatcher: delegates to mms-http-send (primary headless path)
# Usage: mms-send <phone> <file> [mime] [--json]
exec "$(dirname "$0")/mms-http-send" "$@"
